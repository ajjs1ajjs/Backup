<Window x:Class="NovaBackup.GUI.Views.InstantRecoveryWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding WizardTitle}" Height="500" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="#FF1E1E1E"
        Foreground="White"
        TextElement.Foreground="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF2D2D2D" Padding="20">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="LightningBolt" Width="30" Height="30" Margin="0,0,15,0" VerticalAlignment="Center" Foreground="#FF00A8E6"/>
                <StackPanel>
                    <TextBlock Text="{Binding WizardTitle}" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CurrentStep, StringFormat='Step {0} of 3'}" Foreground="#FFAAAAAA"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="1" Padding="30">
            <ContentControl>
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <!-- Step 1: Select RP -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="1">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Select Restore Point" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <TextBlock Text="Source VM" Margin="0,0,0,5"/>
                                            <ComboBox ItemsSource="{Binding AvailableJobs}" SelectedItem="{Binding SelectedJob}" 
                                                      DisplayMemberPath="Name" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,15"/>
                                            
                                            <TextBlock Text="Restore Point" Margin="0,0,0,5"/>
                                            <ListBox ItemsSource="{Binding RestorePoints}" SelectedItem="{Binding SelectedRestorePoint}" Height="150" Background="#FF2D2D2D" BorderThickness="0">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <materialDesign:PackIcon Kind="CalendarClock" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding CreatedAt}"/>
                                                            <TextBlock Text="{Binding Type, StringFormat=' ({0})'}" Foreground="#FFAAAAAA"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </StackPanel>
                                    </Setter.Value> 
                                </Setter>
                            </DataTrigger>
                            <!-- Step 2: Target Settings -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="2">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Recovery Settings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <TextBlock Text="Recovered VM Name" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding VmName}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15"/>
                                            
                                            <TextBlock Text="Target Hypervisor" Margin="0,0,0,5"/>
                                            <StackPanel Orientation="Horizontal">
                                                <RadioButton Content="Hyper-V (Local)" IsChecked="True" Foreground="White" Margin="0,0,20,0"/>
                                                <RadioButton Content="VMware vCenter" Foreground="White" IsEnabled="False"/>
                                            </StackPanel>
                                            
                                            <TextBlock Text="vPower NFS write cache" Margin="0,20,0,5" Foreground="#FFAAAAAA"/>
                                            <TextBox Text="C:\ProgramData\NovaBackup\vPowerCache" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Foreground="#FFAAAAAA"/>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <!-- Step 3: Summary -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="3">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Ready to Recover" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <TextBlock Text="The VM will be published via vPower NFS and registered on the target host." TextWrapping="Wrap" Margin="0,0,0,15"/>
                                            
                                            <Border Background="#FF2D2D2D" Padding="15" CornerRadius="5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <TextBlock Grid.Row="0" Text="Source VM: " Foreground="#FFAAAAAA"/>
                                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedJob.Name}" FontWeight="Bold"/>

                                                    <TextBlock Grid.Row="1" Text="New VM Name: " Foreground="#FFAAAAAA" Margin="0,10,0,0"/>
                                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding VmName}" FontWeight="Bold" Margin="0,10,0,0"/>

                                                    <TextBlock Grid.Row="2" Text="Host: " Foreground="#FFAAAAAA" Margin="0,10,0,0"/>
                                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding RecoveryType}" FontWeight="Bold" Margin="0,10,0,0"/>
                                                </Grid>
                                            </Border>
                                            
                                            <TextBlock Text="{Binding StatusMessage}" Foreground="#FF00A8E6" Margin="0,20,0,0" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#FF2D2D2D" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="CANCEL" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                        HorizontalAlignment="Left" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#FFAAAAAA"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="BACK" Command="{Binding PreviousStepCommand}" Margin="0,0,10,0" Visibility="{Binding CurrentStep, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=1}"/>
                    
                    <Button Content="NEXT" Command="{Binding NextStepCommand}" Visibility="{Binding CurrentStep, Converter={StaticResource IntToVisibilityInverseConverter}, ConverterParameter=3}"/>
                    
                    <Button Content="RECOVER" Command="{Binding FinishCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                            Style="{StaticResource MaterialDesignRaisedAccentButton}" Visibility="{Binding CurrentStep, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=3}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
