<Window x:Class="NovaBackup.GUI.Views.AuditLogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Audit Log" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#FF1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Audit Log" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <Button Content="Refresh" Command="{Binding LoadDataCommand}" Margin="30,0,0,0" Padding="15,5"/>
        </StackPanel>

        <!-- Filter -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Filter:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="300" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" 
                     Background="#FF3D3D3D" Foreground="White" Padding="5"/>
        </StackPanel>

        <!-- Status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="#FF97A4" Margin="0,0,0,10" VerticalAlignment="Top"/>

        <!-- Audit Log Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Entries}" 
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,30,0,0"
                  Background="#FF2D2D2D" Foreground="White" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                <DataGridTextColumn Header="User" Binding="{Binding User}" Width="120"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="150"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Button Grid.Row="3" Content="Close" HorizontalAlignment="Right" Padding="20,5" Click="CloseButton_Click" Margin="0,10,0,0"/>
    </Grid>
</Window>
