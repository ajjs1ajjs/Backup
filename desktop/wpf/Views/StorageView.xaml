<UserControl x:Class="NovaBackup.GUI.Views.StorageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:NovaBackup.GUI.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance Type=viewmodels:StorageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">

    <Grid Background="#FF1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding PageTitle}" FontSize="28" FontWeight="Bold" Margin="30,20" Foreground="White"/>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="30,0,30,15">
            <Button Content="Add Repository" Background="#FF0078D4" BorderThickness="0" Padding="15,5" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Rescan" Background="#FF6C757D" BorderThickness="0" Padding="15,5" Foreground="White" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- Repositories Table -->
        <Border Grid.Row="2" Background="#FF2D2D2D" CornerRadius="4" Padding="20" Margin="30,0,30,30">
            <DataGrid x:Name="dgRepositories" AutoGenerateColumns="False" IsReadOnly="True" 
                      ItemsSource="{Binding Repositories}"
                      Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#FF3D3D3D" RowBackground="Transparent">
                
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Name" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5,0">
                                    <materialDesign:PackIcon Kind="ServerNetwork" Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="#FF00A8E6"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                    <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="2*"/>
                    <DataGridTextColumn Header="Capacity (GB)" Binding="{Binding CapacityGB, StringFormat='\{0:N0\}'}" Width="120"/>
                    <DataGridTextColumn Header="Used (GB)" Binding="{Binding UsedGB, StringFormat='\{0:N0\}'}" Width="120"/>
                    <DataGridTextColumn Header="Free (GB)" Binding="{Binding FreeGB, StringFormat='\{0:N0\}'}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
