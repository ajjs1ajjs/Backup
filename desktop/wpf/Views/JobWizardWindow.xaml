<Window x:Class="NovaBackup.GUI.Views.JobWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding WizardTitle}" Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#FF1E1E1E"
        Foreground="White"
        TextElement.Foreground="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Progress -->
        <Border Grid.Row="0" Background="#FF2D2D2D" Padding="20">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AutoFix" Width="30" Height="30" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="{Binding WizardTitle}" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CurrentStep, StringFormat='Step {0} of 6'}" Foreground="#FFAAAAAA"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="1" Padding="30">
            <ContentControl>
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <!-- Step 1: General -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="1">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Name and Description" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <TextBlock Text="Name" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding JobName}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15"/>
                                            <TextBlock Text="Description" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding JobDescription}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="100" TextWrapping="Wrap" AcceptsReturn="True"/>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <!-- Step 2: Source -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="2">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Select Backup Source" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <ListBox ItemsSource="{Binding AvailableSources}" SelectedItem="{Binding SelectedSource}" Height="200" Background="#FF2D2D2D" BorderThickness="0">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <materialDesign:PackIcon Kind="Server" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding Name}"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <!-- Step 3: Destination -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="3">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Select Backup Destination" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <ListBox ItemsSource="{Binding AvailableRepositories}" SelectedItem="{Binding SelectedRepository}" Height="200" Background="#FF2D2D2D" BorderThickness="0">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <materialDesign:PackIcon Kind="Database" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding Name}"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <!-- Step 4: Schedule -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="4">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Backup Schedule" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <ComboBox SelectedValue="{Binding ScheduleType}" SelectedValuePath="Content" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,20">
                                                <ComboBoxItem Content="Daily"/>
                                                <ComboBoxItem Content="Weekly"/>
                                                <ComboBoxItem Content="Monthly"/>
                                                <ComboBoxItem Content="Continuous"/>
                                            </ComboBox>
                                            <TextBlock Text="Start Time" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding ScheduleTime}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="100" HorizontalAlignment="Left" Margin="0,0,0,20"/>
                                            <TextBlock Text="Retention (Days)" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding RetentionDays}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="100" HorizontalAlignment="Left"/>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <!-- Step 5: Guest Processing -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="5">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Guest OS Processing" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <CheckBox Content="Enable application-aware processing" IsChecked="{Binding EnableGuestProcessing}" Margin="0,0,0,15"/>
                                            <StackPanel IsEnabled="{Binding EnableGuestProcessing}">
                                                <TextBlock Text="Guest Credentials" Margin="0,0,0,5"/>
                                                <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,10">
                                                    <ComboBoxItem Content="Administrator (Local)"/>
                                                    <ComboBoxItem Content="NOVA\svc_backup (Domain)"/>
                                                </ComboBox>
                                                <TextBlock Text="Applications" Margin="0,0,0,5" FontWeight="Bold"/>
                                                <CheckBox Content="Truncate SQL Server logs" IsChecked="True" Margin="0,0,0,5"/>
                                                <CheckBox Content="VSS copy backup (no log truncation)" Margin="0,0,0,5"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <!-- Step 6: Summary -->
                            <DataTrigger Binding="{Binding CurrentStep}" Value="6">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <StackPanel>
                                            <TextBlock Text="Summary" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                
                                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Job Name: " FontWeight="Bold" Margin="0,5"/>
                                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding JobName}" Margin="10,5"/>
                                                
                                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Source: " FontWeight="Bold" Margin="0,5"/>
                                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedSource.Name}" Margin="10,5"/>
                                                
                                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Repository: " FontWeight="Bold" Margin="0,5"/>
                                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedRepository.Name}" Margin="10,5"/>
                                                
                                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Schedule: " FontWeight="Bold" Margin="0,5"/>
                                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ScheduleType}" Margin="10,5"/>

                                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Retention: " FontWeight="Bold" Margin="0,5"/>
                                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding RetentionDays, StringFormat='\{0\} days'}" Margin="10,5"/>

                                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Guest Processing: " FontWeight="Bold" Margin="0,5"/>
                                                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding EnableGuestProcessing}" Margin="10,5"/>
                                            </Grid>
                                            <TextBlock Text="{Binding StatusMessage}" Foreground="#FF00A8E6" Margin="0,20,0,0" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>

        <!-- Footer / Controls -->
        <Border Grid.Row="2" Background="#FF2D2D2D" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="CANCEL" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                        HorizontalAlignment="Left" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#FFAAAAAA"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="BACK" Command="{Binding PreviousStepCommand}" Margin="0,0,10,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="1">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- NEXT Button -->
                    <Button Content="NEXT" Command="{Binding NextStepCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="6">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- FINISH Button -->
                    <Button Content="FINISH" Command="{Binding FinishCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedAccentButton}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStep}" Value="6">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
