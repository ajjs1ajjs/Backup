<UserControl x:Class="NovaBackup.GUI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:NovaBackup.GUI.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">

  <Grid Background="#FF1E1E1E">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Dashboard Header -->
    <TextBlock Grid.Row="0" Text="Dashboard" FontSize="28" FontWeight="Bold" Margin="30,20" Foreground="White"/>

    <!-- Dashboard Content -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <Grid Margin="30,0,30,30">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status Card -->
        <Border Grid.Column="0" Grid.Row="0" Background="#FF2D2D2D" CornerRadius="4" Padding="20" Margin="0,0,15,15">
          <StackPanel>
            <TextBlock Text="System Status" FontSize="16" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="{Binding SystemStatus}" FontSize="12" Foreground="#FFAAAAAA"/>
          </StackPanel>
        </Border>

        <!-- Activity Card -->
        <Border Grid.Column="1" Grid.Row="0" Background="#FF2D2D2D" CornerRadius="4" Padding="20" Margin="0,0,15,15">
          <StackPanel>
            <TextBlock Text="Recent Activity" FontSize="16" FontWeight="Bold" Foreground="White"/>
            <ItemsControl ItemsSource="{Binding RecentActivity}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding}" FontSize="12" Foreground="#FFAAAAAA"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </Border>

        <!-- Recovery Card -->
        <Border Grid.Column="2" Grid.Row="0" Background="#FF2D2D2D" CornerRadius="4" Padding="20" Margin="0,0,0,15">
          <StackPanel>
            <TextBlock Text="Active Recovery" FontSize="16" FontWeight="Bold" Foreground="White"/>
            <ItemsControl ItemsSource="{Binding ActiveSessions}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding VMName}" FontSize="12" Foreground="#FFAAAAAA"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </Border>

        <!-- Quick Actions -->
        <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" Background="#FF2D2D2D" CornerRadius="4" Padding="20" Margin="0,15,0,0">
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Instant Recovery" Style="{StaticResource MaterialDesignRaisedAccentButton}" Width="200" Height="60" Margin="0,0,20,0"/>
            <Button Content="New Backup Job" Style="{StaticResource MaterialDesignRaisedButton}" Width="180" Height="60"/>
          </StackPanel>
        </Border>
      </Grid>
    </ScrollViewer>
  </Grid>
</UserControl>
