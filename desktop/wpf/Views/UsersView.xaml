<Window x:Class="NovaBackup.GUI.Views.UsersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Management" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" Background="#FF1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <Button Content="Refresh" Command="{Binding LoadDataCommand}" Margin="30,0,0,0" Padding="15,5"/>
        </StackPanel>

        <!-- Create User Form -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="New User:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" 
                     Background="#FF3D3D3D" Foreground="White" Padding="5"/>
            
            <TextBlock Grid.Column="2" Text="Email:" Foreground="White" VerticalAlignment="Center" Margin="10,0,10,0"/>
            <TextBox Grid.Column="3" Text="{Binding NewEmail, UpdateSourceTrigger=PropertyChanged}" 
                     Background="#FF3D3D3D" Foreground="White" Padding="5"/>
            
            <Button Grid.Column="5" Content="Create User" Command="{Binding CreateUserCommand}" Padding="15,5"/>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="#FF97A4" Margin="0,0,0,10" VerticalAlignment="Top"/>

        <!-- Users Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" 
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,30,0,0"
                  Background="#FF2D2D2D" Foreground="White" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Roles" Binding="{Binding RoleIds}" Width="150"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding Active}" Width="80"/>
                <DataGridTextColumn Header="Last Login" Binding="{Binding LastLogin}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Delete Selected" Command="{Binding DeleteUserCommand}" Margin="0,0,10,0" Padding="15,5"/>
            <Button Content="Close" Padding="20,5" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
